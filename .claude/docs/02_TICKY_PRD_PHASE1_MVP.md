# Ticky PRD - Phase 1: MVP (Month 1-3)

**버전:** 1.0  
**기간:** Month 1 ~ Month 3 (12주)  
**목표:** 핵심 기능 완성, 베타 테스트 준비

---

## 1. Phase 1 개요

### 1.1 목표

- 영상 제작 협업의 핵심 워크플로우 구현
- 50명 규모 베타 테스트 가능한 제품 완성
- 핵심 가치 검증 (행정 자동화, 버전 관리, 시각적 피드백)

### 1.2 범위 요약

| 기능 영역 | 포함 | 제외 |
|-----------|------|------|
| 인증/인가 | 회원가입, 로그인, 역할 관리 | 2FA, SSO |
| 프로젝트 | CRUD, 상태 관리 | 템플릿, 복제 |
| 문서 | 템플릿 기반 생성, 전자서명 | AI 추천 |
| 영상 | 업로드, 버전 목록, 나란히 비교 | 슬라이더, 오버레이 |
| 피드백 | 기본 주석 도구, 상태 관리 | 고급 도구, 음성 메모 |
| 대시보드 | 기본 현황, 칸반 | 고급 분석, 차트 |
| 알림 | 이메일, 인앱 | Slack, 푸시 |
| 캔버스 | - | 전체 |
| 결제 | - | 전체 |

### 1.3 마일스톤

| 마일스톤 | 시점 | 내용 |
|----------|------|------|
| M1 | Week 4 | 프로젝트 셋업, 인증 완료 |
| M2 | Week 8 | 문서/영상 기본 기능 완료 |
| M3 | Week 10 | 피드백/대시보드 완료 |
| M4 | Week 12 | MVP 완성, 내부 테스트 |

---

## 2. Sprint 1-2: 프로젝트 셋업 & 인증 (Week 1-4)

### 2.1 기술 환경 구축

**프론트엔드**:
- React 18 + TypeScript
- Vite 빌드 환경
- TailwindCSS + 디자인 시스템
- React Router v6
- Zustand 상태관리
- React Query (서버 상태)

**백엔드**:
- Node.js + NestJS
- Supabase 연동
- API 구조 설계
- 환경 변수 관리

**인프라**:
- Cloudflare Pages (프론트엔드)
- Cloudflare Workers (API)
- Cloudflare R2 (스토리지)
- Supabase (DB, Auth, Realtime)

### 2.2 인증/인가 시스템

**사용자 인증**:

| 기능 | 설명 | 우선순위 |
|------|------|----------|
| 이메일 회원가입 | 이메일/비밀번호 가입 | 필수 |
| 이메일 로그인 | 이메일/비밀번호 로그인 | 필수 |
| 소셜 로그인 | Google, Kakao | 필수 |
| 비밀번호 재설정 | 이메일 인증 후 변경 | 필수 |
| 로그아웃 | 세션 종료 | 필수 |
| 이메일 인증 | 회원가입 시 인증 | 필수 |

**사용자 프로필**:

```typescript
interface User {
  id: string;          // UUID
  email: string;
  name: string;
  avatar_url?: string;
  role: 'client' | 'worker' | 'admin';
  created_at: Date;
  updated_at: Date;
}
```

**역할 기반 접근 제어 (RBAC)**:

| 역할 | 프로젝트 생성 | 영상 업로드 | 피드백 작성 | 승인 | 설정 |
|------|-------------|------------|------------|------|------|
| admin | ✓ | ✓ | ✓ | ✓ | ✓ |
| worker | ✓ | ✓ | ✓ | - | - |
| client | ✓ | - | ✓ | ✓ | - |

### 2.3 프로젝트 관리

**프로젝트 데이터 구조**:

```typescript
interface Project {
  id: string;
  title: string;
  description?: string;
  status: 'planning' | 'production' | 'review' | 'settlement';
  stage_status: string;
  client_id: string;
  worker_id?: string;
  deadline?: Date;
  progress: number; // 0-100
  created_at: Date;
  updated_at: Date;
}
```

**프로젝트 상태 흐름**:

```
planning (기획)
├── draft: 초안 작성 중
├── pending: 검토 대기
└── confirmed: 계약 확정

production (제작)
└── in_progress: 작업 진행 (0-100%)

review (검수)
├── first_review: 1차 검수
├── revision: 수정 중
└── approved: 최종 승인

settlement (정산)
├── invoice_sent: 견적 발송
├── payment_pending: 입금 대기
└── completed: 완료
```

### 2.4 Sprint 1-2 산출물

- [ ] 프로젝트 보일러플레이트
- [ ] 인증 시스템 완료
- [ ] 프로젝트 CRUD API
- [ ] 프로젝트 목록/상세 UI
- [ ] 기본 레이아웃 (사이드바, 헤더)
- [ ] 기본 UI 컴포넌트

---

## 3. Sprint 3-4: 문서 관리 (Week 5-8)

### 3.1 문서 템플릿 시스템

**문서 유형**:

| 유형 | 코드 | 용도 |
|------|------|------|
| 작업 요청서 | request | 프로젝트 요청 시 |
| 견적서 | estimate | 비용 산정 |
| 계약서 | contract | 계약 체결 |

**기본 템플릿 필드**:

작업 요청서:
- 프로젝트명 (text, required)
- 작업 유형 (select: 편집/제작/기타)
- 작업 내용 (textarea, required)
- 희망 일정 (date)
- 참고 자료 (file upload)
- 비고 (textarea)

견적서:
- 프로젝트명 (text, required)
- 작업 내용 (textarea, required)
- 예상 일정 (date range)
- 작업 단가 (number)
- 수정 횟수 (number)
- 총 견적 금액 (number, required)
- 결제 조건 (textarea)
- 비고 (textarea)

계약서:
- 프로젝트명 (text, required)
- 갑 (의뢰인) 정보 (text, required)
- 을 (작업자) 정보 (text, required)
- 계약 금액 (number, required)
- 작업 범위 (textarea, required)
- 납품 일정 (date, required)
- 수정 횟수 (number)
- 저작권 조항 (textarea)
- 기타 조항 (textarea)

### 3.2 문서 생성/편집

**문서 데이터 구조**:

```typescript
interface Document {
  id: string;
  project_id: string;
  template_id: string;
  type: 'request' | 'estimate' | 'contract';
  title: string;
  content: Record<string, any>; // 필드별 값
  status: 'draft' | 'pending' | 'approved' | 'rejected' | 'signed';
  version: number;
  file_url?: string;
  reject_reason?: string;
  created_by: string;
  created_at: Date;
  updated_at: Date;
}
```

**문서 워크플로우**:

```
[작성] draft
   ↓ 제출
[검토대기] pending
   ↓ 검토
[승인] approved ←→ [반려] rejected
   ↓ (계약서만)      ↓
[서명] signed    [수정 후 재제출]
```

### 3.3 전자서명

**서명 데이터**:

```typescript
interface Signature {
  id: string;
  document_id: string;
  user_id: string;
  signature_data: string; // base64 이미지
  ip_address: string;
  user_agent: string;
  signed_at: Date;
}
```

**서명 UI**:
- Canvas 기반 서명 패드
- 터치/마우스 지원
- 저장된 서명 불러오기
- 서명 미리보기

### 3.4 Sprint 3-4 산출물

- [ ] 문서 템플릿 CRUD
- [ ] 문서 생성/편집 UI
- [ ] 문서 워크플로우 (상태 전환)
- [ ] 전자서명 기능
- [ ] PDF 내보내기
- [ ] 문서 목록/검색

---

## 4. Sprint 5-6: 영상 버전 관리 (Week 5-8, 병렬)

### 4.1 영상 업로드

**업로드 사양**:

| 항목 | 값 |
|------|-----|
| 지원 포맷 | MP4, MOV, WebM |
| 최대 크기 | 2GB |
| 최대 해상도 | 4K (3840x2160) |
| 최대 길이 | 60분 |

**청크 업로드**:
- 10MB 단위 청크
- 재시도 로직 (최대 3회)
- 진행률 표시
- 중단/재개 지원

**영상 버전 데이터**:

```typescript
interface VideoVersion {
  id: string;
  project_id: string;
  version_number: number;
  version_name: string; // v1_YYYYMMDD_HHmm
  original_filename: string;
  file_url: string;
  preview_url?: string;
  thumbnail_url: string;
  duration: number; // 초
  resolution: string;
  file_size: number;
  codec: string;
  change_notes: string;
  status: 'uploading' | 'processing' | 'ready' | 'error';
  uploaded_by: string;
  created_at: Date;
}
```

### 4.2 영상 처리 파이프라인

```
[업로드 시작] status: uploading
      ↓
[업로드 완료] 
      ↓
[처리 시작] status: processing
      ↓
[메타데이터 추출] duration, resolution, codec
      ↓
[썸네일 생성] 10% 지점 캡처
      ↓
[완료] status: ready
```

### 4.3 영상 플레이어

**기본 기능**:
- 재생/일시정지
- 시크 (드래그, 클릭)
- 볼륨 조절
- 전체화면
- 재생 속도 (0.5x, 1x, 1.5x, 2x)
- 타임코드 표시 (HH:MM:SS)

**피드백 연동**:
- 피드백 마커 표시 (타임라인)
- 마커 클릭 → 해당 시점 이동
- 현재 프레임 캡처

### 4.4 버전 비교 (나란히 재생)

```
┌─────────────────┬─────────────────┐
│     v1 영상      │     v2 영상      │
│                 │                 │
│ [플레이어]       │ [플레이어]       │
│                 │                 │
├─────────────────┴─────────────────┤
│     [동기화 재생 컨트롤]            │
│     ▶️ ━━━━━━━━━━━━━━━━━━━━━━━━━━  │
└───────────────────────────────────┘
```

### 4.5 Sprint 5-6 산출물

- [ ] 청크 업로드 시스템
- [ ] 영상 메타데이터 추출
- [ ] 썸네일 생성
- [ ] 영상 플레이어 컴포넌트
- [ ] 버전 목록 UI
- [ ] 버전 비교 (나란히 재생)
- [ ] 다운로드 기능

---

## 5. Sprint 7-8: 피드백 시스템 (Week 9-10)

### 5.1 주석 도구

**기본 도구**:

| 도구 | 아이콘 | 단축키 | 설명 |
|------|--------|--------|------|
| 선택 | ↖️ | V | 주석 선택/이동 |
| 화살표 | → | A | 방향 지시 |
| 사각형 | □ | R | 영역 강조 |
| 원 | ○ | O | 포인트 강조 |
| 자유 그리기 | ✏️ | P | 펜 도구 |
| 텍스트 | T | T | 텍스트 입력 |

**스타일 옵션**:
- 색상: 빨강, 노랑, 초록, 파랑, 보라, 검정
- 두께: 2px, 4px, 6px, 8px
- 투명도: 50%, 75%, 100%

### 5.2 피드백 CRUD

**피드백 데이터**:

```typescript
interface Feedback {
  id: string;
  video_version_id: string;
  parent_id?: string; // 답글용
  timecode_start: number; // 초
  timecode_end?: number;
  frame_image_url: string;
  content: string;
  annotations: Annotation[];
  status: 'pending' | 'in_progress' | 'review' | 'completed';
  is_urgent: boolean;
  created_by: string;
  resolved_by?: string;
  resolved_at?: Date;
  created_at: Date;
  updated_at: Date;
}

interface Annotation {
  id: string;
  type: 'arrow' | 'rect' | 'circle' | 'freehand' | 'text';
  data: {
    x: number;
    y: number;
    color: string;
    strokeWidth: number;
    opacity: number;
    // 타입별 추가 속성
  };
}
```

### 5.3 피드백 상태 관리

| 상태 | 색상 | 설명 | 전환 가능 |
|------|------|------|-----------|
| pending | 🔴 | 등록됨, 확인 전 | in_progress |
| in_progress | 🟡 | 작업 중 | review, pending |
| review | 🔵 | 검토 대기 | completed, in_progress |
| completed | 🟢 | 완료 | (재오픈: pending) |

### 5.4 Sprint 7-8 산출물

- [ ] 주석 도구 (Canvas 기반)
- [ ] 프레임 캡처 기능
- [ ] 피드백 CRUD API
- [ ] 피드백 상태 관리
- [ ] 피드백 목록 UI
- [ ] 타임라인 마커
- [ ] 댓글/답글 기능

---

## 6. Sprint 9-10: 대시보드 & 알림 (Week 11-12)

### 6.1 대시보드 레이아웃

```
┌────────────────────────────────────────────────────────────┐
│ [로고] 대시보드          검색...      [알림 🔔] [프로필]    │
├──────┬─────────────────────────────────────────────────────┤
│      │                                                     │
│ 사이드│  ┌─────────┬─────────┬─────────┬─────────┐        │
│  바  │  │ 전체     │ 진행중   │ 검토중   │ 완료    │        │
│      │  │   15    │    8    │    2    │   5    │        │
│ ○ 대시│  └─────────┴─────────┴─────────┴─────────┘        │
│   보드│                                                     │
│      │  프로젝트 목록                                       │
│ ○ 프로│  ┌───────────────────────────────────────────┐     │
│   젝트│  │ [프로젝트 A] 진행중 70% ▓▓▓▓▓▓▓░░░ D-5    │     │
│      │  └───────────────────────────────────────────┘     │
└──────┴─────────────────────────────────────────────────────┘
```

### 6.2 대시보드 위젯

**상단 카운터**:
- 전체 프로젝트 수
- 진행중 프로젝트 수
- 검토중 프로젝트 수
- 완료 프로젝트 수

**프로젝트 목록**:
- 프로젝트명
- 현재 상태/단계
- 진행률 바
- 마감일 (D-day)
- 미해결 피드백 수

### 6.3 알림 시스템

**알림 트리거**:

| 이벤트 | 수신자 | 채널 |
|--------|--------|------|
| 새 프로젝트 초대 | 초대받은 사용자 | 이메일, 인앱 |
| 문서 상태 변경 | 관련 사용자 | 인앱 |
| 새 영상 버전 | 의뢰인 | 이메일, 인앱 |
| 새 피드백 | 작업자 | 이메일, 인앱 |
| 긴급 피드백 | 작업자 | 이메일, 인앱 |
| 마감 임박 | 프로젝트 멤버 | 이메일, 인앱 |

**알림 데이터**:

```typescript
interface Notification {
  id: string;
  user_id: string;
  type: string;
  title: string;
  content: string;
  link: string;
  is_read: boolean;
  created_at: Date;
}
```

### 6.4 Sprint 9-10 산출물

- [ ] 대시보드 레이아웃
- [ ] 상태별 카운터
- [ ] 프로젝트 목록 (필터, 정렬)
- [ ] 알림 시스템 (인앱)
- [ ] 알림 시스템 (이메일)
- [ ] 알림 설정 UI

---

## 7. 공통 요구사항

### 7.1 반응형 디자인

| 디바이스 | 범위 | 레이아웃 |
|----------|------|----------|
| 모바일 | < 640px | 1컬럼, 사이드바 숨김 |
| 태블릿 | 640px ~ 1023px | 2컬럼, 축소 사이드바 |
| 데스크톱 | ≥ 1024px | 풀 레이아웃 |

### 7.2 에러 처리

- 네트워크 에러: 재시도 버튼
- 인증 에러: 로그인 페이지 이동
- 권한 에러: 권한 없음 안내
- 유효성 에러: 필드별 메시지

### 7.3 로딩 상태

- 전체 페이지: 스켈레톤 UI
- 부분 로딩: 스피너
- 버튼 로딩: 버튼 내 스피너

---

## 8. API 명세 (주요)

### 8.1 인증

```
POST /auth/signup          # 회원가입
POST /auth/login           # 로그인
POST /auth/logout          # 로그아웃
POST /auth/password/reset  # 비밀번호 재설정
GET  /auth/me              # 현재 사용자 정보
```

### 8.2 프로젝트

```
GET    /projects           # 프로젝트 목록
POST   /projects           # 프로젝트 생성
GET    /projects/:id       # 프로젝트 상세
PATCH  /projects/:id       # 프로젝트 수정
DELETE /projects/:id       # 프로젝트 삭제
POST   /projects/:id/invite # 멤버 초대
```

### 8.3 문서

```
GET    /projects/:id/documents       # 문서 목록
POST   /projects/:id/documents       # 문서 생성
GET    /documents/:id                # 문서 상세
PATCH  /documents/:id                # 문서 수정
PATCH  /documents/:id/status         # 상태 변경
POST   /documents/:id/sign           # 서명
```

### 8.4 영상

```
GET    /projects/:id/videos          # 버전 목록
POST   /projects/:id/videos/upload   # 업로드 시작
PUT    /videos/:id/chunk             # 청크 업로드
POST   /videos/:id/complete          # 업로드 완료
GET    /videos/:id                   # 버전 상세
```

### 8.5 피드백

```
GET    /videos/:id/feedbacks         # 피드백 목록
POST   /videos/:id/feedbacks         # 피드백 생성
PATCH  /feedbacks/:id                # 피드백 수정
PATCH  /feedbacks/:id/status         # 상태 변경
POST   /feedbacks/:id/reply          # 답글 생성
```

### 8.6 알림

```
GET    /notifications                # 알림 목록
PATCH  /notifications/:id/read       # 읽음 처리
PATCH  /notifications/read-all       # 전체 읽음
```

---

## 9. Phase 1 체크리스트

### Week 1-2
- [ ] 프로젝트 환경 구축
- [ ] Supabase 셋업
- [ ] 기본 UI 컴포넌트

### Week 3-4
- [ ] 인증 시스템 완료
- [ ] 프로젝트 CRUD
- [ ] 기본 레이아웃

### Week 5-6
- [ ] 문서 템플릿 시스템
- [ ] 문서 워크플로우
- [ ] 전자서명

### Week 7-8
- [ ] 영상 업로드
- [ ] 영상 플레이어
- [ ] 버전 비교

### Week 9-10
- [ ] 주석 도구
- [ ] 피드백 시스템
- [ ] 대시보드

### Week 11-12
- [ ] 알림 시스템
- [ ] 반응형 최적화
- [ ] 버그 수정 & 테스트
- [ ] MVP 완성

---

**Phase 1 완료 기준**:
1. 모든 핵심 기능 구현 완료
2. 주요 플로우 E2E 테스트 통과
3. 반응형 디자인 완료
4. 성능 기준 충족 (로딩 3초 이내)
5. 내부 테스트 완료
