# Claude Code 개발 요청서 - Phase 3 Sprint 19-20

## 커뮤니티 + 분석

**기간**: Week 1-4 (Month 7-8)
**목표**: 협업 타임라인, AI 피드백 요약, Q&A 커뮤니티, 고급 분석 대시보드

---

## 작업 1: 협업 타임라인

### 요청 내용

```
프로젝트별 모든 활동을 기록하고 표시하는 타임라인 기능을 구현해주세요.

DB 스키마:

-- 00016_activity_logs.sql

CREATE TABLE activity_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  user_id UUID NOT NULL REFERENCES profiles(id),
  type VARCHAR(50) NOT NULL,
  action VARCHAR(50) NOT NULL,
  target_type VARCHAR(50), -- 'video_version', 'feedback', 'document', 'member'
  target_id UUID,
  metadata JSONB DEFAULT '{}',
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_activity_logs_project ON activity_logs(project_id, created_at DESC);
CREATE INDEX idx_activity_logs_user ON activity_logs(user_id);

-- 활동 타입
-- type: 'version', 'feedback', 'document', 'member', 'project', 'chat'
-- action: 'created', 'updated', 'deleted', 'resolved', 'approved', 'signed', 'joined', 'left'

활동 자동 기록:

// lib/activity.ts
export async function logActivity({
  projectId,
  userId,
  type,
  action,
  targetType,
  targetId,
  metadata,
}: ActivityLogParams) {
  await supabase.from('activity_logs').insert({
    project_id: projectId,
    user_id: userId,
    type,
    action,
    target_type: targetType,
    target_id: targetId,
    metadata,
  });
}

// 각 API에서 호출
// 예: 피드백 생성 시
await logActivity({
  projectId,
  userId: user.id,
  type: 'feedback',
  action: 'created',
  targetType: 'feedback',
  targetId: feedback.id,
  metadata: {
    video_version_id: versionId,
    timecode: feedback.timecode,
  },
});

API:

# 활동 목록 조회
GET /api/projects/:projectId/activities
- query: {
    page?: number,
    limit?: number,
    type?: string, // 'feedback', 'version', 'document', 'member'
    user_id?: string,
    start_date?: string,
    end_date?: string
  }
- response: {
    data: ActivityLog[],
    total: number,
    has_more: boolean
  }

UI:

프로젝트 상세 > 활동 탭
┌─────────────────────────────────────────────────────────────┐
│ 활동                                                        │
│ [전체 ▼]  [모든 멤버 ▼]              [2026.01 ▼]           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ 오늘                                                        │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🎬 14:30  김작업자가 v3을 업로드했습니다                 │ │
│ │           브랜드 홍보영상 - 최종본                       │ │
│ │                                                         │ │
│ │ 💬 14:15  이의뢰인이 피드백 3개를 추가했습니다          │ │
│ │           00:15, 00:45, 01:20                           │ │
│ │                                                         │ │
│ │ ✅ 10:00  박매니저가 견적서를 승인했습니다               │ │
│ │           견적서 v2                                      │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 어제 (1월 26일)                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ ✔️ 18:00  김작업자가 피드백 5개를 해결했습니다           │ │
│ │                                                         │ │
│ │ 📄 15:30  시스템이 계약서를 생성했습니다                 │ │
│ │                                                         │ │
│ │ 👤 10:00  박매니저가 프로젝트에 참여했습니다             │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│                        [더 보기]                            │
└─────────────────────────────────────────────────────────────┘

활동 아이콘:
- 🎬 영상 버전
- 💬 피드백
- ✔️ 피드백 해결
- 📄 문서
- ✅ 승인/서명
- 👤 멤버
- 💬 채팅

요구사항:
1. 무한 스크롤
2. 필터링 (타입, 멤버, 기간)
3. 클릭 시 해당 항목으로 이동
4. 실시간 업데이트
5. 그룹화 (같은 사용자 연속 활동)
```

---

## 작업 2: AI 피드백 요약

### 요청 내용

```
긴 피드백 스레드를 AI로 요약하는 기능을 구현해주세요.

API:

# 피드백 요약 생성
POST /api/videos/:videoId/versions/:versionId/feedback-summary
- body: {
    feedback_ids?: string[], // 선택적, 없으면 전체
    include_resolved?: boolean
  }
- response: {
    summary: {
      overview: string,
      key_points: string[],
      action_items: string[],
      unresolved_count: number,
      priority_feedbacks: string[]
    },
    generated_at: Date
  }

구현:

// app/api/.../feedback-summary/route.ts
export async function POST(request: NextRequest) {
  // AI 사용량 체크
  const usageCheck = await checkAIUsage(user.id, 'feedback_summary');
  if (!usageCheck.allowed) {
    return NextResponse.json({
      error: 'AI 사용량 한도에 도달했습니다.',
      remaining: usageCheck.remaining,
    }, { status: 403 });
  }

  // 피드백 조회
  const feedbacks = await getFeedbacks(versionId, feedbackIds);

  // GPT로 요약
  const response = await openai.chat.completions.create({
    model: 'gpt-4o-mini',
    messages: [
      {
        role: 'system',
        content: `당신은 영상 제작 피드백을 분석하는 전문가입니다.
          피드백 목록을 분석하여 다음 정보를 JSON으로 추출하세요:
          - overview: 전체 피드백 요약 (2-3문장)
          - key_points: 핵심 수정 요청 (최대 5개)
          - action_items: 작업자가 해야 할 구체적 작업 목록
          - priority_feedbacks: 우선 처리해야 할 피드백 ID`
      },
      {
        role: 'user',
        content: JSON.stringify(feedbacks.map(f => ({
          id: f.id,
          content: f.content,
          timecode: f.timecode,
          status: f.status,
          priority: f.priority,
        })))
      }
    ],
    response_format: { type: 'json_object' }
  });

  // 사용량 기록
  await recordAIUsage(user.id, 'feedback_summary', response.usage.total_tokens);

  return NextResponse.json({ summary: JSON.parse(response.choices[0].message.content) });
}

UI:

피드백 목록 상단에 요약 버튼:
┌─────────────────────────────────────────────────────────────┐
│ 피드백 (12)                           [✨ AI 요약] [+ 추가] │
├─────────────────────────────────────────────────────────────┤

AI 요약 결과:
┌─────────────────────────────────────────────────────────────┐
│ ✨ AI 피드백 요약                                     [X]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ 📋 개요                                                     │
│ 전체적으로 영상 퀄리티는 좋으나, 자막 관련 수정 요청이      │
│ 가장 많습니다. BGM 볼륨과 색보정 관련 피드백도 있습니다.   │
│                                                             │
│ 🎯 핵심 포인트                                              │
│ • 자막 위치 및 크기 조정 필요 (4건)                         │
│ • BGM 볼륨 전체적으로 낮춰야 함 (2건)                       │
│ • 인트로 색보정 수정 요청 (1건)                             │
│                                                             │
│ ✅ 작업 목록                                                │
│ □ 00:15 ~ 00:30 자막 위치 화면 하단 1/4 지점으로 이동       │
│ □ 00:45 자막 폰트 크기 20% 확대                             │
│ □ 전체 BGM 볼륨 -3dB 조정                                   │
│ □ 00:00 ~ 00:10 인트로 색보정 따뜻하게 수정                 │
│                                                             │
│ ⚠️ 우선 처리                                                │
│ • "자막이 화면 밖으로 잘립니다" (긴급)                       │
│ • "로고 위치 수정 필요" (긴급)                               │
│                                                             │
│                                        [복사하기] [닫기]    │
└─────────────────────────────────────────────────────────────┘

요구사항:
1. Pro 플랜 이상
2. AI 사용량 체크
3. 로딩 상태 표시
4. 결과 복사 기능
5. 재생성 가능
```

---

## 작업 3: Q&A 게시판

### 요청 내용

```
영상 제작 관련 질문/답변 커뮤니티를 구현해주세요.

DB 스키마:

-- 00017_community.sql

-- 게시글
CREATE TABLE posts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  type VARCHAR(20) NOT NULL DEFAULT 'question', -- 'question', 'discussion'
  title VARCHAR(255) NOT NULL,
  content TEXT NOT NULL,
  category VARCHAR(100),
  author_id UUID NOT NULL REFERENCES profiles(id),
  votes INTEGER DEFAULT 0,
  view_count INTEGER DEFAULT 0,
  answer_count INTEGER DEFAULT 0,
  is_solved BOOLEAN DEFAULT FALSE,
  accepted_answer_id UUID,
  is_pinned BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 게시글 태그
CREATE TABLE post_tags (
  post_id UUID REFERENCES posts(id) ON DELETE CASCADE,
  tag VARCHAR(50),
  PRIMARY KEY (post_id, tag)
);

-- 답변
CREATE TABLE answers (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  post_id UUID NOT NULL REFERENCES posts(id) ON DELETE CASCADE,
  content TEXT NOT NULL,
  author_id UUID NOT NULL REFERENCES profiles(id),
  votes INTEGER DEFAULT 0,
  is_accepted BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 투표
CREATE TABLE votes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES profiles(id),
  target_type VARCHAR(20) NOT NULL, -- 'post', 'answer'
  target_id UUID NOT NULL,
  value INTEGER NOT NULL, -- 1 (up) or -1 (down)
  created_at TIMESTAMPTZ DEFAULT NOW(),
  CONSTRAINT unique_vote UNIQUE (user_id, target_type, target_id)
);

-- 인덱스
CREATE INDEX idx_posts_category ON posts(category);
CREATE INDEX idx_posts_votes ON posts(votes DESC);
CREATE INDEX idx_posts_created ON posts(created_at DESC);
CREATE INDEX idx_answers_post ON answers(post_id);
CREATE INDEX idx_post_tags_tag ON post_tags(tag);

-- 트리거: 답변 수 자동 업데이트
CREATE OR REPLACE FUNCTION update_answer_count()
RETURNS TRIGGER AS $$
BEGIN
  IF TG_OP = 'INSERT' THEN
    UPDATE posts SET answer_count = answer_count + 1 WHERE id = NEW.post_id;
  ELSIF TG_OP = 'DELETE' THEN
    UPDATE posts SET answer_count = answer_count - 1 WHERE id = OLD.post_id;
  END IF;
  RETURN NULL;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER answers_count_trigger
AFTER INSERT OR DELETE ON answers
FOR EACH ROW EXECUTE FUNCTION update_answer_count();

API:

# 게시글 목록
GET /api/community/posts
- query: {
    page?, limit?,
    category?: string,
    tag?: string,
    type?: 'question' | 'discussion',
    sort?: 'recent' | 'votes' | 'unanswered',
    search?: string
  }
- response: { data: Post[], total: number }

# 게시글 생성
POST /api/community/posts
- body: { title, content, type, category?, tags? }
- response: { post: Post }

# 게시글 상세
GET /api/community/posts/:id
- response: { post: Post, answers: Answer[] }

# 게시글 수정
PATCH /api/community/posts/:id
- body: { title?, content?, category?, tags? }
- response: { post: Post }

# 게시글 삭제
DELETE /api/community/posts/:id

# 답변 추가
POST /api/community/posts/:id/answers
- body: { content }
- response: { answer: Answer }

# 답변 채택
POST /api/community/posts/:id/accept/:answerId
- response: { post: Post, answer: Answer }

# 투표
POST /api/community/vote
- body: { target_type: 'post' | 'answer', target_id: string, value: 1 | -1 }
- response: { votes: number }

UI:

커뮤니티 메인 (/community)
┌─────────────────────────────────────────────────────────────┐
│ 커뮤니티                                      [질문하기 +] │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ [전체] [질문] [토론]     🔍 검색...                         │
│                                                             │
│ 카테고리                                                    │
│ [전체] [촬영] [편집] [색보정] [사운드] [기타]               │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 📌 [공지] NAVIG 커뮤니티 이용 가이드                     │ │
│ │     NAVIG · 1월 15일 · 조회 1,234                        │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ [12]  프리미어에서 자막 위치 일괄 변경하는 방법?         │ │
│ │  ↑   ✅ 해결됨  편집  #프리미어 #자막                    │ │
│ │  ↓   김작업자 · 2시간 전 · 답변 3 · 조회 45              │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ [8]   다빈치 리졸브 vs 프리미어, 색보정 어떤게 좋나요?  │ │
│ │  ↑   토론  색보정  #다빈치리졸브 #프리미어               │ │
│ │  ↓   이편집자 · 5시간 전 · 답변 12 · 조회 234           │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│                        [더 보기]                            │
└─────────────────────────────────────────────────────────────┘

게시글 상세:
┌─────────────────────────────────────────────────────────────┐
│ ← 목록                                                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ [12]  프리미어에서 자막 위치 일괄 변경하는 방법?            │
│  ↑                                                          │
│  ↓   ✅ 해결됨                                              │
│                                                             │
│ 안녕하세요, 프리미어 프로 사용 중입니다.                    │
│ 자막이 100개 정도 있는데, 위치를 한번에 바꾸고 싶습니다.   │
│ 모션 그래픽 템플릿으로 만들었는데 일괄 변경이 안되네요...   │
│                                                             │
│ 김작업자 · 2시간 전 · #프리미어 #자막                       │
│                                                             │
│ ─────────────────────────────────────────────────────────  │
│                                                             │
│ 답변 3개                                     [최신순 ▼]     │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ ✅ 채택된 답변                                          │ │
│ │                                                         │ │
│ │ [8] Essential Graphics 패널에서 편집하시면 됩니다.       │ │
│ │  ↑                                                      │ │
│ │  ↓  1. 자막 클립 선택                                   │ │
│ │     2. Essential Graphics 패널 열기                      │ │
│ │     3. 위치 값 변경하면 모든 인스턴스에 적용됩니다.     │ │
│ │                                                         │ │
│ │     박편집자 · 1시간 전                                  │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ [답변 작성]                                                 │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 답변을 작성하세요...                                    │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                        [답변 등록]          │
└─────────────────────────────────────────────────────────────┘

요구사항:
1. 마크다운 지원
2. 이미지 첨부
3. 태그 자동완성
4. 검색 기능
5. 알림 (답변, 채택)
6. 신고 기능
```

---

## 작업 4: 고급 분석 대시보드

### 요청 내용

```
프로젝트, 작업자, 수익 분석을 위한 고급 대시보드를 구현해주세요.

API:

# 프로젝트 분석
GET /api/analytics/projects
- query: { start_date, end_date, project_ids? }
- response: {
    overview: {
      total_projects: number,
      completed_projects: number,
      completion_rate: number,
      avg_duration_days: number
    },
    by_status: { status: string, count: number }[],
    by_month: { month: string, created: number, completed: number }[],
    avg_time_per_stage: { stage: string, avg_days: number }[],
    bottlenecks: { stage: string, avg_stuck_days: number }[]
  }

# 피드백 분석
GET /api/analytics/feedbacks
- query: { start_date, end_date, project_ids? }
- response: {
    overview: {
      total_feedbacks: number,
      resolved_feedbacks: number,
      resolution_rate: number,
      avg_response_time_hours: number,
      avg_resolution_time_hours: number
    },
    by_status: { status: string, count: number }[],
    by_type: { type: string, count: number }[],
    trend: { date: string, created: number, resolved: number }[],
    top_creators: { user_id: string, name: string, count: number }[]
  }

# 작업자 분석 (Team 플랜)
GET /api/analytics/workers
- query: { start_date, end_date, worker_ids? }
- response: {
    workers: [{
      user_id: string,
      name: string,
      projects_count: number,
      feedbacks_resolved: number,
      avg_response_time_hours: number,
      on_time_rate: number,
      client_satisfaction?: number
    }],
    comparison: {
      avg_response_time: number,
      avg_on_time_rate: number
    }
  }

# 수익 분석 (Team 플랜)
GET /api/analytics/revenue
- query: { start_date, end_date }
- response: {
    overview: {
      total_revenue: number,
      avg_per_project: number,
      total_projects: number
    },
    by_month: { month: string, revenue: number, projects: number }[],
    by_client: { client_id: string, name: string, revenue: number, projects: number }[],
    by_type: { type: string, revenue: number }[]
  }

UI:

분석 대시보드 (/analytics)
┌─────────────────────────────────────────────────────────────┐
│ 분석                                   [2026년 1월 ▼] [PDF] │
├─────────────────────────────────────────────────────────────┤
│ [프로젝트] [피드백] [작업자] [수익]                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ 프로젝트 현황                                               │
│ ┌───────────┬───────────┬───────────┬───────────┐          │
│ │   전체    │   진행중   │   완료    │ 완료율    │          │
│ │    45    │    12    │    30    │   67%    │          │
│ └───────────┴───────────┴───────────┴───────────┘          │
│                                                             │
│ 프로젝트 추이                              평균 소요 시간   │
│ ┌─────────────────────────────────┐  ┌─────────────────────┐│
│ │     생성 ── 완료                │  │ 전체: 14일          ││
│ │  8 ┤    ╱╲                      │  │                     ││
│ │  6 ┤   ╱  ╲   ╱╲                │  │ 기획: 3일           ││
│ │  4 ┤  ╱    ╲ ╱  ╲               │  │ 제작: 7일           ││
│ │  2 ┤ ╱                          │  │ 검토: 4일           ││
│ │    └─────────────────────────── │  └─────────────────────┘│
│ └─────────────────────────────────┘                         │
│                                                             │
│ 단계별 병목                                                 │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 검토 단계에서 평균 4일 지연 (업계 평균 대비 +1일)        │ │
│ │ → 피드백 응답 시간 개선 필요                             │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘

피드백 분석 탭:
┌─────────────────────────────────────────────────────────────┐
│ 피드백 분석                                                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ┌───────────┬───────────┬───────────┬───────────┐          │
│ │  전체     │  해결     │ 해결률    │ 평균응답   │          │
│ │   234    │   198    │   85%    │  2.5h    │          │
│ └───────────┴───────────┴───────────┴───────────┘          │
│                                                             │
│ 피드백 추이                              상태별 분포        │
│ ┌─────────────────────────────────┐  ┌─────────────────────┐│
│ │ 차트                            │  │ 대기: 15%           ││
│ │                                 │  │ 진행: 5%            ││
│ │                                 │  │ 해결: 80%           ││
│ └─────────────────────────────────┘  └─────────────────────┘│
│                                                             │
│ Top 피드백 작성자                                           │
│ 1. 김의뢰인 (45건)  2. 이의뢰인 (32건)  3. 박의뢰인 (28건) │
│                                                             │
└─────────────────────────────────────────────────────────────┘

요구사항:
1. recharts 라이브러리 사용
2. 기간 필터 (주, 월, 분기, 년)
3. PDF 내보내기
4. 데이터 캐싱 (성능)
5. 플랜별 접근 제한 (작업자/수익 분석은 Team)
6. 비교 기간 선택
```

---

## 참조 파일

- `rules/CODING_STANDARDS.md` - 코딩 컨벤션
- `rules/DESIGN_SYSTEM.md` - 디자인 시스템
- `10_NAVIG_PRD_PHASE2-3_UNIFIED.md` - 통합 PRD 섹션 3.1

---

## 완료 기준

### 기능 체크리스트

**협업 타임라인**
- [ ] 활동 로그 테이블
- [ ] 자동 기록 로직
- [ ] 타임라인 UI
- [ ] 필터/검색
- [ ] 실시간 업데이트

**AI 피드백 요약**
- [ ] GPT 연동
- [ ] 요약 API
- [ ] 결과 UI
- [ ] 사용량 체크

**Q&A 커뮤니티**
- [ ] 게시글 CRUD
- [ ] 답변 CRUD
- [ ] 투표 기능
- [ ] 채택 기능
- [ ] 검색/필터

**고급 분석**
- [ ] 분석 API
- [ ] 차트 컴포넌트
- [ ] 필터 기능
- [ ] PDF 내보내기

### 품질 체크리스트

- [ ] 성능 최적화 (캐싱)
- [ ] 플랜별 접근 제한
- [ ] 반응형 디자인
- [ ] 에러 핸들링
